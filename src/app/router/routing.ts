const routing = {
  apps: '/',
  app: '/apps/:appName',
  services: '/services',
  service: '/services/:host/:serviceName',
  git: '/git',
  logs: '/logs',
  test: '/test',
};

export default routing;
